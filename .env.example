# Session cookie signing secret (lib/auth-config.ts getSessionSecret). Required
# in production (NODE_ENV=production); outside production a fixed development
# secret is used automatically, so dev, Vitest and CI need no value here.
SESSION_SECRET=
